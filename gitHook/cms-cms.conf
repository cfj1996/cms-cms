upstream hook_server {
    server 127.0.0.1:3000;
    keepalive 2000;
}
server {
        listen 80 default_server;

        server_name admin.platypus.art;

        access_log /var/log/nginx/cms-access.log timed_combined;
	      error_log /var/log/nginx/cms-error.log;

        location / {
                root   /root/woerk/174-2/dist;
                try_files $uri $uri/ /index.html;
                index  index.html index.htm;
        }

        # location /api {
        #         proxy_send_timeout 300;
        #         proxy_read_timeout 300;
        #         proxy_pass https://p4010174-u848-6abfd459.api.tiancai.run;
        # }
        location /webHook/ {
                proxy_pass http://hook_server/;
            }

}
